const fila=document.getElementById("cordx");
const columna=document.getElementById("cordy");

const posision=function corde(x,y) {
    let filas=[1,2,3,4,5,6,7,8]
    "cordx"=filas;
    let columnas=[1,2,3,4,5,6,7,8]
    "cordy"=columnas;
    if (x!==filas, y!==columnas) {
        alert("!no hay valores validos!");
    } else{
        alert("calculando movimientos");
    }
}

function verificar() {
    corde("cordx","cordy")
}